dist: xenial
language: python

branches:
  except:
    - gh-pages

python:
  - '3.7'
  #- '3.8'


install:
  - pipenv install --dev --pre --skip-lock

script:
  - python -m boken.api.tests
  - python -m boken.ws.tests
  - pytest tests
  -

notifications:
  email: false

